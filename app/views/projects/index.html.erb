<h1>All Projects</h1>
<% @projects.each do |project| %>
  <h4><%= link_to project.title, project %></h4>
  <small><%= project.description %></small>
<% end %>

<%= link_to "New Project Folder", new_project_path, class: 'btn btn-success' %>
